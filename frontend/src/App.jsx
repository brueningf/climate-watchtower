export default function App() {
  return (
    <div style={{fontFamily: 'system-ui, sans-serif', padding: 24}}>
      <h1>Climate Watchman — Frontend</h1>
      <p>This is a minimal Vite + React app. API requests should be prefixed with <code>/api</code> and will be proxied to the Spring Boot backend during development.</p>
    </div>
  )
}

